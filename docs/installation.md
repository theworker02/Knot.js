# Installation

```bash
npm install -g @magnexis/knotjs
```

or, without a global install:

```bash
npx @magnexis/knotjs --help
```

Requires Node.js 20.10 or later. TypeScript entrypoints need Node.js 22.6+ (type stripping).

The global store defaults to `~/.knot`. Override with `KNOT_STORE`.
